// export const getDataDescOrder = (timelineData) => {
//     const copiedData = [...timelineData];

//     const newTimeLineData = copiedData.sort((dataA, dataB) => {
//       const dateA = new Date(`${dataA.date}`);
//       const dateB = new Date(`${dataB.date}`);
//       return dateB - dateA;
//     });

//     return newTimeLineData;
//   };
